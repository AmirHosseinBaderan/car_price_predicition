# پیش‌بینی قیمت خودرو

یک پروژه یادگیری ماشین که با استفاده از الگوریتم K-نزدیک‌ترین همسایه (KNN) قیمت خودروها را پیش‌بینی می‌کند.

## چه کاری انجام می‌دهد

این پروژه قیمت خودروها را بر اساس ویژگی‌های خودرو پیش‌بینی می‌کند. با داشتن مشخصات یک خودرو (مایلیج، برند، مدل، تریم، نوع، سیلیندر، لیتر)، قیمت تخمین‌زده شده را همراه با معیارهای ارزیابی (MAE و RMSE) برمی‌گرداند.

## چگونه کار می‌کند

کلاس `CarPricePredictor` در فایل `car_price_predictor.py` یک مدل رگرسیون KNN را پیاده‌سازی می‌کند:

1. **بارگذاری داده‌ها** - داده‌های خودرو را از فایل اکسل (`cars_data.xls`) می‌خواند که شامل ۸۰۴ رکورد خودرو با ستون‌های: Price, Mileage, Make, Model, Trim, Type, Cylinder, Liter, Doors, Cruise, Sound, Leather

2. **آماده‌سازی ویژگی‌ها** - ویژگی‌های دسته‌ای (برند، مدل، تریم، نوع) را با استفاده از رمزنگاری یک‌گیسه (`pd.get_dummies()`) به بردارهای عددی تبدیل می‌کند

3. **نرمال‌سازی** - ویژگی‌های عددی (مایلیج، سیلیندر، لیتر) را با کم کردن مقدار میانگین و تقسیم بر انحراف استاندارد مقیاس‌بندی می‌کند

4. **پیش‌بینی** - با استفاده از k=۵ همسایه نزدیک‌ترین خودروها را در فضای ویژگی پیدا می‌کند و میانگین قیمت آن‌ها را برمی‌گرداند

5. **ارزیابی** - با استفاده از اعتبارسنجی رها-یک-ساز (leave-one-out)، میانگین خطای مطلق (MAE) و جذر میانگین خطای مربعاتی (RMSE) را محاسبه می‌کند

## پیاده‌سازی الگوریتم KNN

KNN الگوریتم یادگیری ساده‌ای است که در اینجا برای رگرسیون استفاده شده:

- **محاسبه فاصله**: از فاصله اقلیدسی برای سنجش شباهت بین خودروها در فضای ویژگی نرمال‌سازی شده استفاده می‌کند
- **انتخاب همسایه**: برای پیش‌بینی، k=۵ خودرو نزدیک‌ترین (همنشیان) را به خودرو ورودی پیدا می‌کند
- **پیش‌بینی**: قیمت پیش‌بینی شده، میانگین قیمت همسایه‌های k است
- **آموزش**: فاز آموزش واضح ندارد - مدل فقط تمام بردارهای ویژگی را ذخیره می‌کند
- **اعتبارسنجی متقاطع**: متد `evaluate()` از اعتبارسنجی رها-یک-ساز استفاده می‌کند، که در آن هر خودرو با پیدا کردن همسایه‌ها در میان تمام خودروهای دیگر دیتاست پیش‌بینی می‌شود

مزیت اصلی KNN سادگی و قابلیت تفسیرپذیری آن است - پیش‌بینی‌ها بر اساس خودروهای واقعی مشابه از دیتاست است، نه پارامترهای یادگرفته شده.

## مرجع API

### `CarPricePredictor(excel_file: str, k: int = 5)`

سازنده که پیش‌بینی‌کننده را با مسیر فایل داده اکسل و تعداد همسایه‌ها (k) می‌سازد.

### `__init__(self, excel_file: str, k: int = 5)`

مقداردهی اولیه پیش‌بینی‌کننده با:
- `excel_file`: مسیر فایل اکسل حاوی داده‌های خودرو
- `k`: تعداد همسایه‌های نزدیک‌ترین (پیش‌فرض: ۵)

### `load(self)`

فایل اکسل را در یک DataFrame پانداس می‌خواند و اعتبارسنجی می‌کند که ستون‌های مورد نیاز وجود داشته باشند. ستون‌های مورد نیاز: Price, Mileage, Make, Model, Trim, Type, Cylinder, Liter. ردیف‌های حاوی مقادیر گمشده را حذف می‌کند.

### `prepare(self)`

ویژگی‌ها را برای پیش‌بینی آماده می‌کند:
- رمزنگاری یک‌گیسه ستون‌های دسته‌ای (Make, Model, Trim, Type)
- نرمال‌سازی ستون‌های عددی (Mileage, Cylinder, Liter) با استفاده از نرمال‌سازی z-score
- ذخیره ستون‌های ویژگی و پارامترهای مقیاس‌بندی (means, stds) برای استفاده بعدی

### `fit(self)`

متد راحتی که `load()` و `prepare()` را به ترتیال صدا می‌کند.

### `build_feature_vector(self, car: dict) -> np.ndarray`

یک دیکشنری خودرو را به بردار ویژگی نرمال‌سازی شده تبدیل می‌کند که با داده‌های آموزشی سازگار است:
- رمزنگاری یک‌گیسه را روی ویژگی‌های دسته‌ای اعمال می‌کند
- ویژگی‌ها را با ستون‌های دیتاست آموزشی هم‌راستا می‌کند (ویژگی‌های گمشده با ۰ پر می‌شود)
- همان نرمال‌سازی استفاده شده در آموزش را اعمال می‌کند

### `predict(self, car: dict) -> float`

قیمت یک خودرو را پیش‌بینی می‌کند:
- با استفاده از `build_feature_vector()` بردار ویژگی ساخته می‌شود
- فواصل اقلیدسی به تمام نمونه‌های آموزشی محاسبه می‌شود
- k همسایه نزدیک‌تر پیدا می‌شود
- میانگین قیمت آن همسایه‌ها برمی‌گردد

### `evaluate(self)`

عملکرد مدل را با اعتبارسنجی رها-یک-ساز (leave-one-out) ارزیابی می‌کند:
- برای هر خودرو، قیمت را با پیدا کردن همسایه‌ها در میان بقیه دیتاست پیش‌بینی می‌کند
- MAE (Mean Absolute Error) و RMSE (Root Mean Squared Error) را محاسبه و چاپ می‌کند

## استفاده

```bash
python main.py
```

اسکریپت اصلی مدل را آموزش می‌دهد، ارزیابی می‌کند و قیمت یک خودرو Toyota Corolla نمونه با ۱۲۰,۰۰۰ مایل و موتور ۱.۸ لیتر، ۴ سیلیندر را پیش‌بینی می‌کند.

## پیش‌نیازها

- pandas
- numpy
- xlrd (برای خواندن فایل اکسل)

## فایل‌ها

- `main.py` - نقطه ورود برنامه
- `car_price_predictor.py` - پیاده‌سازی کلاس CarPricePredictor
- `cars_data.xls` - دیتاست با ۸۰۴ رکورد خودرو